# 위험도 분석(risk-analysis) 도메인 — 구현 현황 정리

## 배경 / 성격

다른 문서들과 같은 방식의 **회고성(retroactive) 문서**입니다. 이전 버전에서는 "코드 자체가 전혀 없음"으로 정리했지만, 이후 `com.algogyeyak.riskanalysis` 패키지에 **골격(스켈레톤) 코드**가 추가되었고, **(2026-08-04 완료)** 신호 탐지기 4종 + `RiskAnalysisController` + market-data 어댑터(전세)에 이어 `DepositSafetyCheckService`(전세가율 계산), 매매 실거래가 연동, checklist 연계 보조 신호, 매물 수정 시 자동 재계산 트리거까지 전부 구현 완료됐습니다 — risk-analysis 도메인의 API 4개(`POST /risk-analysis`, `GET /risk-signals`, `GET /deposit-safety`, `POST /deposit-safety/recalculate`)가 모두 실제로 동작하고, 완전 미해결 이슈는 더 이상 없습니다. 남은 건 "동일 계정 다수 등록 탐지"를 실제로 켤지(팀 결정 대기, 로직은 이미 완성)와 "선순위보증금 입력을 어느 화면에 배치할지"(FE/UX 결정) 두 가지뿐이며, 둘 다 코드 작업이 아니라 결정만 남은 상태입니다.

## 실제 구현 현황: risk-analysis 4개 API 전부 완료 (2026-08-04 갱신)

### 준비된 것

- **엔티티**
  - `PropertyRisk` — **매물×신호타입 조합당 1행**(`(property_id, signal_type)` unique 제약)으로 최신 탐지 결과 1건(`description`)을 기록. **(변경)** `risk_check_id`(FK)는 제거됨 — `PropertyRiskCheck`와 같은 자연키(`property_id`+`signal_type`)를 공유하고 같은 트랜잭션에서 upsert되므로 대리키로 연결할 필요가 없어짐(기존에도 어디서도 읽히지 않던 write-only 컬럼이었음). `detectedAt`도 제거(새 스키마에 없음). `property`는 `@ManyToOne`+`@JoinColumn`으로 실제 FK — 이전엔 `propertyId`를 bare `Long` 컬럼으로만 들고 JPA 연관관계를 맺지 않았음.
  - `PropertyRiskCheck` — **매물×신호타입 조합당 1행**(`(property_id, signal_type)` unique 제약)으로 최신 체크 상태를 관리. `RiskCheckStatus`(SUCCESS/UNDETERMINABLE/FAILED) + `RiskCheckReason`(판정불가/실패 사유)을 신호 4종마다 독립적으로 갖고, 재계산 시 새로 만들지 않고 `overwrite()`로 같은 행을 덮어씀. (이전 버전은 `property_id` 단독 unique라 매물 전체에 상태 하나뿐이었는데, 신호별로 분리됨 — 아래 오케스트레이션 서비스 참고) **(변경)** `property`도 `PropertyRisk`와 마찬가지로 bare `Long propertyId`에서 `@ManyToOne`+`@JoinColumn` 실제 FK로 변경됨 — `checklist.Checklist`가 이미 쓰던 컨벤션과 통일.
  - `DepositSafetyCheck` — 매물당 1행(`property_id` 단독 unique, `@ManyToOne` FK)으로 전세가율 계산 결과를 관리. `jeonseRatio`(계산 결과, CALCULATED일 때만 존재, **percent 정수로 반올림해서 저장**)·`seniorDeposit`/`maxClaimAmount`(선택 입력, 반영된 경우에만 값 존재)·`explanation`·`policyVersion`을 갖고, `DepositSafetyStatus`(CALCULATED/UNAVAILABLE/FAILED) + `DepositSafetyCheckReason`(판정불가/실패 사유)으로 `PropertyRiskCheck`와 동일한 상태 모델을 따름. 정적 팩토리(`calculated`/`unavailable`/`failed`) + `overwrite()`로 재계산 시 덮어쓰는 패턴도 동일. **(2026-08-04 추가)** `referenceDate`(계산에 사용한 매매 실거래가 표본 중 최신 계약일) 컬럼 추가 — GET 응답에 "오래된 정보 최신처럼 보이지 않게" 표시하기 위한 원본 요구사항 항목. **(2026-08-04 완료)** 이 엔티티를 실제로 채우는 계산 로직(`DepositSafetyCheckService`)까지 구현 완료 — 데이터 모델만 준비됐던 상태에서 벗어남.
- **enum** — `RiskSignalType`(4종: 가격이상치/중복매물/동일계정다수등록/짧은주기재등록), `RiskCheckStatus`, `RiskCheckReason`, **(신규)** `DepositSafetyStatus`(CALCULATED/UNAVAILABLE/FAILED), **(신규)** `DepositSafetyCheckReason`(전세가율 판정불가/실패 사유 — `RiskCheckReason`과 같은 패턴으로 신설), `MarketComparisonStatus`, `MarketUnavailableReason`(risk-analysis가 market-data 응답을 소비할 때 쓰는 자체 정의 — market-data 도메인의 실제 enum이 아님).
- **DTO** — `MarketComparison`(risk-analysis 전용 뷰 record, market-data의 엔티티가 아님을 주석으로 명시), `SignalCheckResult`(status/reason/description — 탐지기 하나가 자신의 SUCCESS/UNDETERMINABLE/FAILED와 설명 1건을 독립적으로 표현하기 위해 신설). **(변경)** `description`은 원래 `List<DetectedSignal>`이었으나, `property_risks`가 `(property_id, signal_type)` unique 제약을 갖도록 스키마가 확정되면서(신호 하나당 row 1개만 허용) 단일 `String`으로 축소됨 — 탐지기가 여러 건을 찾아도(예: 중복매물 2건) 하나의 문장으로 합쳐서 반환해야 함. 더는 안 쓰이는 `DetectedSignal` DTO는 삭제됨. **(2026-08-03 구현 완료)** `RiskSignalResponse`(signalType/status/reason/description/checkedAt) — `PropertyRiskCheck`(상태)와 `PropertyRisk`(리스크 발견 시 설명)를 signalType 기준으로 묶어 응답하는 `from(check, risk)` 팩토리를 가짐. **(2026-08-04 추가)** `RiskAnalysisSummaryResponse`(propertyId/signalCount/policyVersion/calculatedAt) — `POST /risk-analysis` 전용 요약 응답. `propertyId`는 GET `RiskSignalListResponse`와 대칭을 맞추기 위해 추가됨(처음엔 없었다가 같은 날 바로 추가). `RiskSignalListResponse`(propertyId/signalCount/signals/disclaimer) — `GET /risk-signals` 전용 응답으로, `signals`는 신호 4종 전부(각자 status/reason 포함)를 담되 `signalCount`는 그중 실제 리스크가 발견된 것만 센다. 두 응답 모두 API 명세서 초안을 그대로 따른 것 — `policyVersion`은 `RiskAnalysisSummaryResponse`에만 있고(요약 수준에서 "정책 버전 기록" 요구사항을 충족) 개별 `RiskSignalResponse`에는 넣지 않기로 결정. **(2026-08-04 완료)** `DepositSafetyCheckResponse`(propertyId/status/jeonseRatio/seniorDepositApplied/seniorDeposit/maxClaimAmount/explanation/referenceDate/reason/calculatedAt/disclaimer) — `RiskSignalResponse`와 같은 패턴으로 status/reason/값 필드를 하나의 shape에 담아 별도 "판정불가 응답 모양"을 두지 않는다. `GET /deposit-safety`와 `POST /deposit-safety/recalculate` 응답에 동일하게 재사용됨. `seniorDeposit`/`maxClaimAmount`는 처음엔 "항상 null인 죽은 필드"라 뺐다가, `recalculate` 구현 시점에 다시 추가함(반영된 경우에만 값이 채워짐). `DepositSafetyRecalculateRequest`(seniorDeposit 필수 ≥0, maxClaimAmount 선택 ≥0, Bean Validation) 신규.
- **repository** — `PropertyRiskRepository`(`findByPropertyIdAndSignalType`로 upsert 대상 단건 조회, `findAllByPropertyId`로 매물의 신호 결과 전체 조회, 신호가 더 이상 감지되지 않을 때 지우는 `deleteByPropertyIdAndSignalType`), `PropertyRiskCheckRepository`(`findByPropertyIdAndSignalType`로 신호별 단건 조회, `findAllByPropertyId`로 매물의 4개 신호 결과 전체 조회). 두 리포지토리 모두 `propertyId` 파라미터 타입은 그대로 `Long`이지만, 엔티티가 `property` 연관관계로 바뀌면서 Spring Data가 `property.id`로 자동 해석함(메서드명 변경 불필요 — 컨텍스트 로드 테스트로 확인).
- **정책 설정** — `RiskPolicyConfig`(`@ConfigurationProperties(prefix = "risk-policy")`). `application.yml`에 `risk-policy:` 블록(`version: v1.0`, `price-anomaly-percent: 10`, `jeonse-ratio-warn-from: 100`, `jeonse-ratio-warn-to: 150`, `jeonse-ratio-alert-over: 150`, `multi-account-detection-enabled: false`)이 채워져 있음. **(2026-08-03 추가)** `same-account-threshold-count: 3`, `same-account-window-days: 7`(동일계정 다수등록 판단 기준), `short-term-relisting-window-days: 30`, `short-term-relisting-price-tolerance-percent: 5`, `short-term-relisting-area-tolerance-percent: 10`(짧은 주기 재등록 판단 기준) — 전부 명세서에 정확한 숫자가 없어 잠정값으로 정하고 정책값으로 뺀 것. **(2026-08-03 갱신)** `price-anomaly-percent`도 이제 `PriceAnomalyDetector`가 실제로 소비함 — 죽은 설정값이었던 게 전부 해소됨. **(2026-08-04 추가)** `jeonse-ratio-caution-from: 80` — 전세가율 설명 문구의 안전/주의 경계값(요구사항엔 "80%가 넘으면 위험" 예시만 있고 정식 임계값 항목은 아니었으나, 기존 `jeonseRatioWarnFrom`(100, 위험 경계)과 별개로 필요해 신설). 최종 4단계: 안전(80% 미만)/주의(80~100%)/위험(100~150%, `jeonseRatioWarnFrom~To`)/~~입력값 재확인 안내(150% 초과, `jeonseRatioAlertOver`)~~ **(2026-08-12 정정)** 입력값 재확인 안내(150% 초과)는 실제로는 `jeonseRatioWarnTo`를 넘는 else 분기로만 판정되고, `jeonseRatioAlertOver`는 코드 어디서도 참조되지 않는 죽은 설정값임(전수조사 결과 버그 3번 참고 — 현재는 두 값이 우연히 같아(150) 동작상 문제가 드러나지 않을 뿐).
  - 정책 버전 이력(과거 버전별 임계값을 DB에 남기는 것)은 Github의 `application.yml` 변경 이력을 확인하는 것으로 임시 설정.
- **오케스트레이션 서비스** — `FakeListingSignalService.checkAndSave(Property)`가 구현되어 있음. **신호별 독립 판정 구조로 변경됨**: 이전 버전은 시세비교(`MarketComparison`)가 `FAILED`/`UNDETERMINABLE`이면 4개 탐지기를 아예 돌리지 않고 매물 전체를 판정불가/실패로 기록했는데(중복매물·동일계정·재등록처럼 시세비교와 무관한 신호까지 같이 막혀버리는 문제가 있었음), 지금은 시세비교를 한 번만 조회해서 활성화된(`SignalDetector.isEnabled()`) 탐지기 4개 모두에게 넘기고, 그 결과를 각 탐지기가 `SignalCheckResult`(SUCCESS/UNDETERMINABLE/FAILED + 사유 + 탐지된 신호 목록)로 독립적으로 반환하도록 바뀜. 신호 하나가 판정불가/실패여도 나머지 신호는 영향받지 않고, 결과는 `(propertyId, signalType)` 단위로 `PropertyRiskCheck`에 upsert되고, `PropertyRisk`도 같은 키로 upsert됨(있으면 `description`을 `overwrite()`로 덮어쓰고, 없으면 새로 insert) — 리스크가 해소되거나 판정불가/실패로 바뀌면 기존 row를 삭제(`deleteByPropertyIdAndSignalType`)함. **(변경)** 원래는 매번 delete 후 탐지된 신호 리스트 전체를 재삽입하는 방식이었으나, `property_risks`가 `(property_id, signal_type)` unique 제약을 갖게 되면서 "신호당 row 1개"만 허용돼 이 upsert 방식으로 바뀜 — 다른 신호가 이미 찾아둔 결과를 덮어쓰지 않는 점은 동일. **(2026-08-03 갱신)** `checkAndSave(Property)` 외에 소유권 체크가 붙은 `checkAndSave(Long userId, Long propertyId)` 오버로드와, 신호 조회용 `getSignals(Long userId, Long propertyId)`가 추가되어 이제 컨트롤러에 연결됨(아래 참고) — "최초 실행"과 "재계산"을 구분하는 별도 메서드/엔드포인트를 두지 않았는데, `checkAndSave`가 이미 upsert 구조라 몇 번을 호출해도 결과가 같기 때문. **(2026-08-04 갱신)** `getSignals()`의 반환 타입이 `List<RiskSignalResponse>` → `RiskSignalListResponse`(propertyId/signalCount/signals/disclaimer)로 바뀜 — 애초엔 `POST /risk-analysis`가 빈 응답만 주던 걸 `checkAndSummarize()`(→ `RiskAnalysisSummaryResponse`)로 고치면서 signalCount 개념이 생겼는데, "POST에 signalCount가 있으면 GET도 같이 있어야 하지 않나"는 판단으로 GET도 같은 개념을 노출하도록 확장함. `checkAndSummarize()`는 `checkAndSave()` 실행 후 `getSignals()`가 이미 계산한 `signalCount`를 그대로 재사용한다(중복 계산 없음). **(2026-08-04 연결 완료)** `checkAndSave(Property)`가 신호 4종 탐지기 실행에 이어 `DepositSafetyCheckService.checkAndSave(property)`도 같이 호출하도록 연결됨 — 원본 요구사항의 "API 목적"(허위매물 의심 신호와 보증금 안전성을 함께 계산)이 이제 `POST /risk-analysis` 하나로 실현됨. 그 전까지는 두 서비스가 서로 참조하지 않아 `GET /deposit-safety`를 호출해도 항상 "계산된 적 없음"만 나오는 상태였음.
- **`SignalDetector`** — `detect()`의 반환 타입이 `List<DetectedSignal>` → `SignalCheckResult`로 변경됨. `comparison`은 여전히 4개 탐지기 모두에게 파라미터로 전달되지만, 실제로 이 값을 참고해야 하는 건 `PriceAnomalyDetector` 하나뿐이고 나머지 3개는 무시하도록 설계됨(인터페이스를 둘로 쪼개는 대신 단일 리스트로 오케스트레이션을 단순하게 유지하는 절충).
- **(2026-08-03 해결) `MarketDataClient`** — risk-analysis가 정의한 인터페이스. 이전엔 임시로 `TemporaryMarketDataClient`(항상 `UNDETERMINABLE` 반환)를 꽂아뒀었는데, 실제 구현체 `MarketDataClientImpl`(`client` 패키지)로 교체 완료 — `TemporaryMarketDataClient`는 삭제됨. `propertyId`로 매물을 조회해 `MarketComparisonService.compare(property)`를 호출하고, market-data의 `MarketComparisonResponse`(2단계 상태 + `MarketComparisonUnavailableReason`)를 risk-analysis의 `MarketComparison`(3단계 상태 + `MarketUnavailableReason`)으로 변환한다. "AVAILABLE"→`SUCCESS`, 그 외 전부→`UNDETERMINABLE`로 매핑(진짜 `FAILED`는 market-data가 API 실패를 구분해 넘겨주지 않아 여전히 다루지 않음).

### 여전히 비어 있는 것

**(2026-08-04 기준) 코드로 채워야 할 빈 부분은 더 이상 없습니다.** 예전엔 이 섹션에 `RiskRecalculationService`(빈 스텁)와 checklist 연계 보조 신호가 있었는데, 둘 다 구현 완료됐습니다(아래 "남은 이슈" 4번/8번 참고). 남은 두 항목("동일 계정 다수 등록" 활성화 여부, 선순위보증금 입력 화면 배치)은 코드가 아니라 팀/FE 결정만 남은 상태입니다.

### market-data 도메인 연동 — 전세 완료(2026-08-03), 매매 완료(2026-08-04)

market-data 도메인(`MarketComparisonService`, `MolitRentClientImpl` 등)은 국토부 실거래가 조회 → 전세만 필터링 → 면적오차 ±20% → 반경 300m→600m 확장 → 중앙값 계산까지 동작하고, `property` 도메인(`PropertyService.register/getProperty/update`)에 연결되어 매물 등록·조회·수정 응답에 실시간으로 반영되고 있습니다. 이건 원래 `property`의 매물 상세/등록 응답용으로 만들어진 것이라 risk-analysis의 `MarketDataClient`·`MarketComparison`과는 형태가 안 맞았는데, `MarketDataClientImpl` 어댑터로 이 격차를 해소했습니다:

- **메서드 시그니처 변환**: `MarketComparisonService.compare(Property)`(엔티티 직접)를 `MarketDataClient.getComparison(Long propertyId)`(ID만) 규격에 맞춰, 어댑터 내부에서 `PropertyRepository.findById()`로 Property를 다시 조회.
- **상태 모델 변환**: market-data의 2단계(`"AVAILABLE"`/`"UNAVAILABLE"`)를 risk-analysis의 3단계로 매핑 — `"AVAILABLE"` → `SUCCESS`, 그 외 전부 → `UNDETERMINABLE`. market-data의 `MarketComparisonUnavailableReason`(`TRANSACTION_TYPE_UNSUPPORTED`/`PROPERTY_TYPE_UNSUPPORTED`/`ADDRESS_INFO_MISSING`/`INSUFFICIENT_SAMPLE`, 2026-08-03에 신규 추가)을 risk-analysis의 `MarketUnavailableReason`으로 매핑.
- **필드 타입 변환**: `referencePrice`(Long→BigDecimal), `differenceRate`(Double→BigDecimal), `referenceDate`(String "yyyy-MM-dd"→LocalDate) 등. `askingPrice`는 market-data 응답에 없어 `Property.getDeposit()`으로 어댑터가 직접 채움.
- **한계로 남은 부분**: `MolitRentClientImpl.fetch()`가 국토부 API 호출 실패 시에도 예외 없이 빈 리스트를 반환하므로(`Collections.emptyList()`), "표본이 원래 없는 경우"와 "API 장애로 못 가져온 경우"가 여전히 구분되지 않고 똑같이 `UNDETERMINABLE`(`INSUFFICIENT_SAMPLE`)로 보입니다. risk-analysis가 요구하는 진짜 `FAILED`(외부 API 장애) 판정은 이번 범위에서 다루지 않기로 결정 — 어댑터는 항상 `UNDETERMINABLE`로만 매핑하고, `FAILED` 분기 자체는 코드에 남겨뒀지만(`PriceAnomalyDetector`가 `MarketComparisonStatus.FAILED`를 받으면 처리하는 로직은 있음) 실제로는 아직 트리거될 일이 없음.
- 참고로 market-data는 매물 조회/수정 때마다 캐싱 없이 즉시 재계산하므로(`marketComparisonService.compare(property)`), market-data 자신에게는 "재계산 트리거" 문제가 없음 — 이건 risk-analysis처럼 결과를 DB에 스냅샷으로 저장하는 쪽에만 해당되는 문제(아래 "남은 이슈" 참고).

**(2026-08-04 신규) 매매 실거래가 연동** — 전세가율(보증금/매매시세) 계산에는 국토부 "매매" 실거래가가 필요한데, 기존 market-data는 "전월세" API만 연동돼 있었음(그마저도 전세만 필터링해서 씀). 팀원이 먼저 `MolitTradeResponse`(매매 API 응답 DTO)를 dev에 추가해뒀고, 이어서 다음을 구현:
- `TradeTransactionSample`/`MolitTradeClient`/`MolitTradeClientImpl` — `RentTransactionSample`/`MolitRentClientImpl`과 완전히 같은 패턴(같은 `molit.service-key`, User-Agent 우회, XML→JSON 파싱)으로 오피스텔/연립다세대/단독다가구 매매 URL 3개(`RTMSDataSvcOffiTrade`/`RTMSDataSvcRHTrade`/`RTMSDataSvcSHTrade`) 호출. 매매 API는 전월세 API와 달리 단독/다가구 `jibun`이 완전 비공개가 아니라 `"1**"`처럼 마스킹돼서 내려옴 — `resolveJibunAddress()`가 `"*"` 포함 여부까지 확인해서 지오코딩 후보에서 제외.
- `MarketSaleComparisonResponse`/`MarketSaleComparisonService`(marketdata 패키지, 신규) — 지역/면적/반경(300m→600m)/중앙값 매칭 알고리즘은 `MarketComparisonService`와 동일하되, **의도적으로 별도 클래스로 중복 구현**(이미 검증된 전세 비교 플로우를 안 건드리기 위한 선택, `risk-analysis-design.md` 리뷰 시 트레이드오프 논의함). 전세 비교와의 차이: (1) 매물의 거래유형(전세/월세)과 무관하게 조회(매매가는 매물이 전세든 월세든 똑같이 필요), (2) 단독/다가구를 하드코딩으로 제외하지 않음(위 마스킹 필터링으로 자연스럽게 표본에서 빠짐), (3) `differenceRate` 없음(매물 자체 가격 비교가 아니라 기준가 자체가 필요).
- `MarketSalePrice`/`MarketSaleDataClient`/`MarketSaleDataClientImpl`(risk-analysis 패키지) — `MarketDataClient`/`MarketDataClientImpl`(전세)과 같은 어댑터 패턴. `DepositSafetyCheckReason`이 "판정불가" 사유를 `ESTIMATED_PRICE_MISSING` 하나로만 다루므로, AVAILABLE이 아니면 전부 `Optional.empty()`로 뭉뚱그림.
- 매매 실거래가 원본 데이터(개별 거래 목록)는 DB에 저장하지 않음 — 전세 쪽과 동일하게 그때그때 조회해서 중앙값만 계산하고 버림. `DepositSafetyCheck` 테이블엔 최종 계산 결과(`jeonseRatio` 등)만 저장.

**(2026-08-04 신규) `DepositSafetyCheckService`** — 보증금과 매매시세로 전세가율을 계산·저장.
- `checkAndSave(Property)`(POST /risk-analysis에서 호출, 선순위보증금 없이 기본 계산)와 `recalculate(userId, propertyId, seniorDeposit, maxClaimAmount)`(POST /deposit-safety/recalculate 전용, 사용자 입력 반영)가 내부적으로 같은 `calculate()`를 공유 — 분자를 `내 보증금 [+ seniorDeposit] [+ maxClaimAmount]`로 구성하고 분모(매매시세)로 나눠 percent 정수로 반올림.
- 판정 순서: 월세(`TRANSACTION_TYPE_UNSUPPORTED`) → 보증금 없음(`DEPOSIT_INFO_MISSING`) → 매매시세 조회 실패(`ESTIMATED_PRICE_MISSING`) → 계산 성공(`CALCULATED`).
- 전세가율 설명 문구 4단계(안전/주의/위험/입력값 재확인)는 `RiskPolicyConfig`의 `jeonseRatioCautionFrom/WarnFrom/WarnTo/AlertOver`로 관리 — 원본 요구사항의 "80% 넘으면 위험" 예시 문구와 "100~150% 위험 강조, 150% 초과 경고" 임계값을 그대로 반영.
- `get(userId, propertyId)`는 조회 전용(계산 트리거 안 함) — 아직 한 번도 계산된 적 없으면(`DepositSafetyCheck` row 없음) `status`가 `null`인 응답을 그대로 반환.
- 구현 중 발견한 설계 이슈: `upsertUnavailable`/`upsertCalculated`가 애초엔 저장 후 `findByPropertyId()`로 재조회해서 응답을 만들었는데, 저장 직후 다시 조회하는 건 프로덕션에서도 불필요한 DB 왕복이라 `calculate()`가 저장된 엔티티를 직접 반환하도록 고침(단위 테스트에서 mock의 `save()` 기본 반환값이 `null`이라는 걸로 발견).
- `GET /properties/{propertyId}/deposit-safety`, `POST /properties/{propertyId}/deposit-safety/recalculate` 컨트롤러(`DepositSafetyController`) 구현 완료 — 둘 다 `DepositSafetyCheckResponse` 하나를 재사용.
- **(#173 완료)** `DepositSafetyCheckResponse`에 판정 근거 데이터를 추가로 노출: `sampleCount`/`radiusMeters`(계산에 쓰인 매매 실거래 표본 수·반경 — `MarketSalePrice`/`DepositSafetyCheck` 엔티티에 컬럼 추가), `cautionFrom`/`warnFrom`/`warnTo`(전세가율 안전/주의/위험 구간 경계값 — `RiskPolicyConfig`에서 그대로 전달). 이전에는 FE가 "전세가율 51%, 안전한 편" 같은 결론만 받고 왜 그런지 근거를 볼 수 없었는데, 이제 "몇 건/반경 몇 m 기준"과 "구간 경계값"을 함께 내려줘 멘토 피드백("판정 기준값·근거 데이터가 안 보인다")을 해소함. FE는 #121에서 이 필드들을 `RiskAnalysisClient.tsx`에 렌더링.

## 다른 도메인에 남아있는 연계 흔적

- `property` 도메인의 매물 상세조회(`PropertyDetailResponse`)엔 위험 신호·안전성 정보를 담을 필드가 없음(`property-design.md` 확인 필요 항목 #3과 동일 사안). **(2026-08-07 확인)** 다만 BE가 이 필드를 채워주는 대신, FE `PropertyDetailClient`가 매물 상세 페이지 로드 시 `GET /properties/{propertyId}/risk-signals`·`GET /properties/{propertyId}/deposit-safety`를 별도로 호출해 화면을 채우고 있어 기능적으로는 이미 연동돼 있음 — `PropertyDetailResponse`에 필드를 추가할 필요는 없어 보임.
- **(2026-08-04 해결)** `checklist` 도메인의 `ChecklistItemCode.OWNERSHIP_ACQUISITION_DATE`(소유권 취득일 문항)에 남아있던 "risk-analysis 전세가율과 연계되는 보조 신호용" 주석이 실제 코드로 이어짐 — `ChecklistItemRepository`에 문항 단건 조회 쿼리를 추가해 `DepositSafetyCheckService`가 직접 읽어온다(아래 "남은 이슈" 4번 참고).
- **(2026-08-03 해결)** `property`의 매물 삭제는 논리 삭제(soft delete, `PropertyStatus.DELETED`)라 삭제된 매물 데이터가 DB에 남아있음 — `ShortTermRelistingDetector`가 이 재료(삭제 이력)를 실제로 활용해 구현 완료됨. 별도 `deletedAt` 컬럼이 없어 `updatedAt`을 삭제 시각 대용으로 사용.
- **(2026-08-03 해결)** `property`의 등록 시 중복 검사(`PROPERTY_DUPLICATE`)는 "동일 사용자가 동일 주소·거래유형으로 재등록"할 때만 걸리는 검사라, `DuplicateListingDetector`가 요구하는 "서비스 전체에서 동일 주소·유사 조건으로 등록된 다른 매물(다른 사용자 포함)" 탐지와는 성격이 달라 재사용이 어려웠음 — `PropertyRepository`에 `existsByIdNotAndTransactionTypeAndStatusAndAddress_RoadAddress`/`_JibunAddress`(userId 조건 없이 본인 자신만 제외) 신규 쿼리를 추가해 해결.

## 요구사항 대비 대조

| 요구사항 항목                                  | 실제 상태                                                                                                                                                               |
|------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 가격 이상치 신호(시세 대비 10~20% 저렴)               | ✅ **(2026-08-03 구현)** `PriceAnomalyDetector` — `MarketDataClientImpl`이 넘겨주는 `MarketComparison.differenceRate()`가 `-priceAnomalyPercent%`(잠정 10%) 이하면 신호. 판정불가/실패 사유도 market-data 쪽 사유를 그대로 매핑해 전달 |
| 동일 주소·유사 조건 중복매물 탐지                      | ✅ **(2026-08-03 구현)** `DuplicateListingDetector` — 동일 주소(도로명 우선, 없으면 지번) + 동일 거래유형으로 다른 계정이 등록한 매물이 있으면 신호. 가격·면적 유사도는 오차 기준이 명세에 없어 이번 범위에서 제외                                                                                                                             |
| 동일 계정 다수 등록 탐지 (🔶 논의중, 정책 플래그로 온/오프 요구) | ⚠️ **(2026-08-03 구현, 기본 비활성)** `SameAccountMultipleDetector` — 최근 `sameAccountWindowDays`(잠정 7일) 이내 동일 계정이 `sameAccountThresholdCount`(잠정 3개) 이상, 서로 다른 지역(지번주소 앞 2토큰)에 매물을 등록했으면 신호. `multiAccountDetectionEnabled`가 여전히 `false`라 실제로는 동작 안 함 — 팀 결정 나면 플래그만 켜면 됨         |
| 짧은 주기 재등록 탐지                             | ✅ **(2026-08-03 구현)** `ShortTermRelistingDetector` — 동일 계정이 삭제한 매물과 동일 주소로, `shortTermRelistingWindowDays`(잠정 30일) 이내 + 가격·면적이 각각 허용 오차(잠정 5%/10%) 이내면 신호                                                                                           |
| 전세가율 계산(기본/선순위보증금 반영/근저당 채권최고액 반영)       | ✅ **(2026-08-04 구현)** `DepositSafetyCheckService.calculate()` — `(내 보증금 [+선순위보증금] [+근저당 채권최고액]) / 매매시세`, percent 정수로 반올림. 근저당 채권최고액은 요구사항대로 별도 할인 없이 그대로 합산. `GET /deposit-safety`(기본 조회)와 `POST /deposit-safety/recalculate`(선순위보증금 반영 재계산)로 노출 |
| 선순위보증금 검증(100~150% 강조 안내, 150% 초과 경고)    | ✅ **(2026-08-04 구현)** `DepositSafetyCheckService.buildExplanation()` — `RiskPolicyConfig`의 `jeonseRatioWarnFrom/To`(100~150, 위험 강조)·~~`jeonseRatioAlertOver`(150, 입력값 재확인 안내) 소비~~ **(2026-08-12 정정)** `jeonseRatioAlertOver`는 실제로 참조되지 않는 죽은 값이고, 150% 초과 판정은 `jeonseRatioWarnTo`를 넘는 else 분기로만 이뤄짐(전수조사 결과 버그 3번 참고). 계산 자체는 막지 않고 설명 문구로만 경고 |
| "최근 소유권 변경 + 높은 전세가율" 보조 신호              | ✅ **(2026-08-04 구현)** `DepositSafetyCheckResponse.recentOwnershipChangeWarning` — 전세가율 위험 구간(100%↑) AND 소유권 취득일 최근(6개월↓) 둘 다 만족해야 true                                                                                                                            |
| 성공/판정불가/실패 3단계 응답 구조                     | ✅ **(2026-08-03 완료)** `PropertyRiskCheck.status`(`RiskCheckStatus`)를 신호 4종마다 독립적으로 관리(`(property_id, signal_type)` unique)해서, 신호 하나가 판정불가/실패여도 나머지 신호 결과에 영향 없음. **(2026-08-04 갱신)** `GET /properties/{propertyId}/risk-signals`가 `RiskSignalListResponse`(propertyId/signalCount/signals/disclaimer)로 클라이언트에 내려주고, `signals` 안에 신호 4종 전부 실제 값을 채움. `POST /properties/{propertyId}/risk-analysis`는 `RiskAnalysisSummaryResponse`(signalCount/policyVersion/calculatedAt)로 요약만 반환 — 이전엔 빈 응답만 줘서 클라이언트가 다시 GET을 호출해야 했던 걸 이번에 고침 |
| 정책 버전 기록                                 | ⚠️ 부분 구현 — `PropertyRiskCheck.policyVersion`에 체크 시점의 `RiskPolicyConfig.version` 문자열을 스냅샷으로 남기지만, 버전별 임계값 이력을 DB에 남기는 기능은 없음                                         |
| 위험 신호 재계산(매물 수정 시 트리거)                   | ✅ **(2026-08-04 구현)** `PropertyService.update()` → `PropertyUpdatedEvent` 발행 → `RiskRecalculationService`(`@TransactionalEventListener(AFTER_COMMIT)`)가 구독해 자동 재계산. 이벤트 기반이라 property는 risk-analysis를 모름           |

## 비기능 요구사항 — 대조

| 항목 | 상태 |
|---|---|
| 동일 입력·동일 정책 버전이면 동일 결과 보장 | ✅ **(2026-08-03 완료)** 탐지기 4종 전부 순수 함수적 판정(DB 조회 + 정책값 비교) — 결정적(deterministic). `PriceAnomalyDetector`도 같은 `MarketComparison` 입력이면 항상 같은 결과 |
| 위험도 계산 실패가 매물 상세 조회 전체 실패로 이어지지 않음 | ✅ **(2026-08-07 확인)** `FakeListingSignalService`는 market-data 실패/판정불가를 예외 없이 상태값으로 흡수한다. BE `PropertyService.getProperty()`는 애초에 risk-analysis를 호출하지 않지만, FE `page.tsx`가 `GET /risk-signals`·`GET /deposit-safety`를 매물 조회와 별개로 호출해 실패해도 `.catch()`로 흡수하고 매물 본문은 그대로 렌더링함(위 "다른 도메인에 남아있는 연계 흔적" 참고) — 결과적으로 요구사항이 만족됨 |
| 실거래가 조회 실패와 위험도 계산 실패 구분 | ⚠️ 모델 수준에서는 `RiskCheckReason.DATA_FETCH_FAILURE`(FAILED)와 `NO_COMPARABLE_TRANSACTION`(UNDETERMINABLE)로 구분되지만, 실제 market-data 구현(`MolitRentClientImpl`)이 외부 API 실패를 예외로 던지지 않고 빈 리스트로 삼켜버려 "실패"와 "판정불가"가 원천 데이터부터 구분되지 않음 — `MarketDataClientImpl` 어댑터도 이 한계를 그대로 물려받아 항상 `UNDETERMINABLE`로만 매핑함(위 'market-data 도메인 연동' 참고). market-data 쪽에서 API 실패를 예외로 구분해 던지는 선행 작업이 있어야 완전히 해결됨 |
| 매물 정보/시세 변경 시에만 재계산(불필요한 재계산 방지) | ✅ **(2026-08-04 구현)** `PropertyUpdatedEvent`가 매물 수정 시에만 발행되므로, 조회만으로는 재계산이 트리거되지 않음 |
| 동일계정 다수등록 탐지를 정책 플래그로 켜고 끌 수 있게 | ✅ **(2026-08-03 구현)** `SameAccountMultipleDetector.isEnabled()`가 `RiskPolicyConfig.multiAccountDetectionEnabled`를 그대로 반환 — 탐지 로직 자체는 완성됐고 플래그로만 켜고 끔(기본값 `false`) |
| 서비스 대상 범위(전세+월세)와 안전성 체크 적용 범위(전세만) 화면 안내 구분 | ⚠️ **(2026-08-04 갱신)** 백엔드는 월세 매물에 `DepositSafetyStatus.UNAVAILABLE`+`TRANSACTION_TYPE_UNSUPPORTED` 사유를 정확히 내려줌(안내에 필요한 데이터는 있음) — 다만 이 사유를 화면에 실제로 안내 문구로 보여주는 건 FE 몫이라 백엔드만으로는 완결되지 않음 |

## 남은 이슈 / 확인 필요 총정리

1. **(2026-08-03 완전 해결)** 신호 탐지 로직 4종(`PriceAnomalyDetector`, `DuplicateListingDetector`, `SameAccountMultipleDetector`, `ShortTermRelistingDetector`) 전부 구현 완료.
2. **(2026-08-03 해결, 2026-08-04 응답 구조 갱신)** `RiskAnalysisController` — `POST /properties/{propertyId}/risk-analysis`(실행/재계산), `GET /properties/{propertyId}/risk-signals`(조회) 구현 완료. **(2026-08-04)** API 명세서 초안을 기준으로 응답 구조를 다시 맞춤 — POST는 빈 응답 대신 `RiskAnalysisSummaryResponse`(signalCount/policyVersion/calculatedAt)를, GET은 배열 대신 `RiskSignalListResponse`(propertyId/signalCount/signals/disclaimer)를 반환. 애초 구현이 자체 판단으로 작성된 것이라 명세서와 어긋나 있던 걸 바로잡은 것.
3. **(2026-08-03 완전 해결)** market-data 연동 — `MarketComparisonResponse`에 구조화된 `MarketComparisonUnavailableReason` enum 추가 + `MarketDataClientImpl` 어댑터 구현 완료(`TemporaryMarketDataClient`는 삭제). 상태 모델(2단계→3단계)·메서드 시그니처·필드 타입 변환 전부 처리됨. 다만 "API 실패"와 "표본 부족" 구분은 market-data 원천 데이터 자체가 못 하고 있어 여전히 못 함(둘 다 `UNDETERMINABLE`로 뭉뚱그려짐) — 진짜 `FAILED` 판정을 원하면 market-data 쪽에 추가 선행 작업 필요.
4. **(2026-08-04 완전 해결)** `checklist` 연계 보조 신호("최근 소유권 변경 + 높은 전세가율") 구현 완료. `ChecklistItemRepository.findByChecklist_Property_IdAndCode()`(체크리스트 전체가 아니라 문항 하나만 가볍게 조회하는 신규 쿼리, 매물당 체크리스트가 최대 1개라 `Optional` 단건으로 충분 — `uk_checklist_user_property` 유니크 제약으로 보장됨)로 `OWNERSHIP_ACQUISITION_DATE` 값을 읽어와, 전세가율이 위험 구간(`jeonseRatioWarnFrom`, 100% 이상)이고 소유권 취득일이 최근(`ownershipRecentChangeMonths`, 잠정 6개월) 이내면 `DepositSafetyCheckResponse.recentOwnershipChangeWarning`을 `true`로 반환한다. 두 조건이 모두 참이어야 하고(AND), 체크리스트 문항에 응답이 없으면(값 `null`) 조용히 `false` 처리한다 — 이 경고는 부가 정보라 판정 불가로 전체 응답을 막을 이유가 없기 때문.
5. **(2026-08-03 해결)** `property`의 기존 중복 등록 검사(동일 사용자 한정)와 `DuplicateListingDetector`가 요구하는 신호(다른 사용자 포함 가능)는 서로 다른 메커니즘이었으나, `PropertyRepository`에 별도 쿼리(`existsByIdNotAnd...`)를 추가해 해결.
6. "동일 계정 다수 등록 탐지"는 요구사항 문서 자체가 🔶 논의중이라고 표시한 항목 — **(2026-08-03 갱신)** 로직 자체는 구현 완료했고 `multiAccountDetectionEnabled` 플래그(기본 `false`)로 게이트만 걸어둔 상태. Property 도메인이 "임대인이 아닌 일반 사용자가 검토용으로 등록"하는 구조라는 점 때문에 이 신호가 원래 의도(임대인/중개사 어뷰징 탐지)대로 작동하지 않을 수 있다는 점은 여전히 팀 결정 대기 — 결정 나면 정확한 임계값(현재 잠정값: 3개/7일/지역 2곳 이상)을 확정하고 플래그만 켜면 됨. **(2026-08-04 방침)** 바로 켤지 말지 결정하기보다, 전반적인 서비스 사용 흐름을 지켜보면서(실사용 데이터로 이 신호가 실제로 유의미하게 작동하는지) 테스트해본 뒤 적용 여부를 판단하기로 함.
7. ~~**(2026-08-04 일부 해소)** "선순위보증금을 매물 등록 시점에 받을지, 임장 체크리스트의 서류·행정 카테고리에서 받을지" 화면 흐름 — `POST /deposit-safety/recalculate`가 매물 등록/수정과 완전히 분리된 별도 엔드포인트로 구현돼서, 사용자가 언제든(등록 직후든, 체크리스트 진행 중이든) 선순위보증금을 입력해 재계산을 호출할 수 있게 됨. 다만 "어느 화면에서 이 입력을 유도할지"는 여전히 FE/UX 결정 필요.~~ — ✅ **(2026-08-19 정정)** 이미 해결됨을 뒤늦게 확인 — FE `RiskAnalysisClient.tsx`(매물 상세 → 위험분석 페이지)에 선순위보증금 입력 폼이 구현돼 있음(숫자 포맷팅, 음수/NaN 검증, `POST /deposit-safety/recalculate` 호출, "선순위보증금·근저당 채권최고액이 뭔가요?" 설명 섹션까지 포함). "여전히 FE/UX 결정 필요"라는 이전 서술은 그 사이 FE 작업이 반영된 뒤 갱신이 안 된 stale 정보였음.
8. **(2026-08-04 완전 해결)** 매물 수정 시 자동 재계산 트리거 구현 완료 — property 담당자와 협의 후 **이벤트 기반**으로 구현하기로 결정(FE 시퀀싱 대안 대신 백엔드 훅으로 확정). `property.event.PropertyUpdatedEvent`(propertyId만 담는 도메인 이벤트)를 `PropertyService.update()`가 발행하고, risk-analysis의 `RiskRecalculationService`(빈 스텁 → 구현)가 `@TransactionalEventListener(phase = AFTER_COMMIT)`로 구독해 `FakeListingSignalService.checkAndSave()`를 실행한다. 직접 서비스 주입 대신 이벤트를 택한 이유 둘: (1) `checkAndSave()`도 `@Transactional`이라 매물 수정과 같은 트랜잭션에 물리면 재계산 실패 시 예외를 잡아도 스프링이 트랜잭션을 이미 rollback-only로 표시해버려 매물 수정 자체가 롤백될 위험이 있음(AFTER_COMMIT은 커밋 후 별도 트랜잭션이라 이 문제가 없음) — "위험도 계산 실패가 매물 상세 조회 전체 실패로 이어지지 않음" 요구사항과 직결. (2) ~~`property` 패키지가 risk-analysis를 import하지 않아도 되어 market-data처럼 어댑터로 참조하는 기존 방향성과도 맞음~~ **(2026-08-12 정정)** 이건 쓰기(재계산 트리거) 경로에만 해당됨 — 읽기 경로는 반대로 `PropertyService`가 매물 목록의 위험신호/전세가율 배지를 위해 risk-analysis의 `PropertyRiskRepository`/`PropertyRiskCheckRepository`/`DepositSafetyCheckRepository`를 직접 import해서 쓰고 있어, 실제로는 결합 방향이 일관되지 않음(전수조사 결과 코드 품질 1번 참고). 재계산 중 예외는 잡아서 로그만 남기고 밖으로 던지지 않는다.
9. **(2026-08-03 해결)** `risk-policy` 설정값(`price-anomaly-percent`, `same-account-*`, `short-term-relisting-*`) 전부 이제 실제 판정 로직이 소비함 — 더 이상 죽은 설정값 없음. 다만 `price-anomaly-percent` 외 값들은 전부 명세서에 정확한 기준이 없어 정한 잠정값이라, 실제 운영하면서 튜닝 필요.
10. **(2026-08-04 신규 이슈, 완전 해결)** 원래 9개 목록엔 없었던 항목 — `DepositSafetyCheckService`(전세가율 계산) + 매매 실거래가 연동(`MolitTradeClientImpl`/`MarketSaleComparisonService`) + `GET /deposit-safety`/`POST /deposit-safety/recalculate` 컨트롤러까지 전부 구현 완료. `POST /risk-analysis`에도 연결돼 신호 4종과 전세가율이 한 번의 호출로 같이 계산됨.
11. **(2026-08-06 신규 이슈, 완전 해결) 동시 요청 시 upsert 유니크 제약 위반 — 트러블슈팅.** auth/admin 담당자가 `fix/auth-samesite_none_aws` 테스트 중 발견해 인계한 문서 기준: `FakeListingSignalService`/`DepositSafetyCheckService`의 upsert("없으면 insert, 있으면 update")는 조회와 insert 사이에 갭이 있는 check-then-act 패턴이라, 같은 매물에 대한 요청 두 개가 동시에 들어오면(React 개발 모드의 이중 `useEffect` 호출, 사용자 조회와 `PropertyUpdatedEvent` 배치 재계산이 겹치는 경우 등) 둘 다 "기존 행 없음"을 보고 동시에 insert를 시도해 유니크 제약 위반(`DataIntegrityViolationException`)이 났다. 팀원이 먼저 커밋한 잠정 조치(`PropertyRepository.findByIdForRiskCheckUpdate()`, `PESSIMISTIC_WRITE` 행 잠금을 `calculate()`/`checkAndSave()` 진입부에서 거는 방식, `dev`에 PR #118로 병합됨)는 경쟁 자체는 막았지만, 이 잠금을 국토부 실거래가 API 호출(전세+매매 비교, 각각 최대 6개월 조회 루프 × 최대 40초 read timeout — 최악의 경우 매물 하나당 최대 12번의 순차 외부 호출) 내내 들고 있어 동시 요청이 몰릴수록 잠금 대기가 길게 직렬화되는 문제가 있었다. 대안으로 락 대신 **REQUIRES_NEW 트랜잭션 격리**를 채택 — insert만 별도 트랜잭션(`TransactionTemplate`+`PROPAGATION_REQUIRES_NEW`)으로 격리해 `saveAndFlush()`가 유니크 제약을 위반해도 그 임시 트랜잭션의 Hibernate 세션만 버려지고 바깥(원래) 트랜잭션의 세션은 오염되지 않게 하며(같은 세션에서 실패 후 계속 쿼리하면 "세션이 예외 이후 flush됨" `AssertionFailure`가 나는 문제를 피함), 실패하면 그 사이 먼저 커밋된 행을 재조회해서 덮어쓴다 — `CustomOAuth2UserService.createUser()`/`LocalAuthService.signup()`/`PropertyReportService.report()`가 이미 쓰고 있던, 이 코드베이스에 이미 검증된 패턴을 그대로 재사용한 것. 이 방식은 잠금 자체가 없어 외부 API 호출 시간과 무관하게 항상 즉시 처리되고, 신규 DB 컬럼(버전 체크용)도 필요 없다. `FakeListingSignalService`(신호 4종 upsert)와 `DepositSafetyCheckService`(전세가율 upsert) 양쪽 모두 적용 완료, `findByIdForRiskCheckUpdate()`는 두 호출부 모두 제거되며 더 이상 쓰이지 않아 삭제됨. 실제 H2 DB로 두 스레드가 동시에 `checkAndSave()`를 호출하는 통합 테스트(`FakeListingSignalServiceConcurrencyTest`)로 새 방식이 경쟁을 실제로 막는지 확인함.
    - **(2026-08-06 추가 발견·수정)** 위 REQUIRES_NEW 복구(재조회 후 덮어쓰기)를 바깥(원래) 트랜잭션에서 그대로 수행하면, MySQL InnoDB의 기본 격리수준인 REPEATABLE READ에서는 그 트랜잭션이 이미 앞서 읽은 시점의 스냅샷에 고정돼 있어 방금 REQUIRES_NEW로 커밋된 승자 행이 재조회에서 안 보일 수 있다는 문제를 발견함(MySQL 공식 문서: "the repeatable read snapshot is created at the start of the first real statement ... and no changes made by later ... transactions"). H2는 기본 격리수준이 READ_COMMITTED라 로컬 동시성 테스트로는 이 문제가 드러나지 않는다 — 이 프로젝트는 Docker/CI가 없어 실제 MySQL로 직접 재현 검증은 못 했고, 문서화된 DB 동작과 코드 흐름 분석에 근거한 방어적 수정임을 명시해둔다. 수정: 복구용 재조회도 별도 REQUIRES_NEW 트랜잭션에서 수행하고(항상 새 스냅샷을 잡으므로 격리수준과 무관하게 승자를 볼 수 있음), 그 안에서 바로 `overwrite()` + `saveAndFlush()`까지 마쳐 커밋한다(재조회와 덮어쓰기를 분리하면 엔티티가 detached 상태가 되어 변경이 저장 안 될 수 있어 하나의 트랜잭션에 묶음). 재조회로도 승자를 못 찾는 경우(이론상 발생하면 안 되는 상황)는 `log.error`로 남기고 원래 예외를 그대로 던진다. `FakeListingSignalService.upsertCheck()`/`upsertRisk()`, `DepositSafetyCheckService.upsertUnavailable()`/`upsertCalculated()` 4곳 모두 적용.
    - **(2026-08-06 범위 밖 발견)** 같은 "REQUIRES_NEW insert + 바깥 트랜잭션에서 재조회 복구" 패턴이 auth 도메인의 `CustomOAuth2UserService.createUser()`/`linkNewSocialAccount()`, `LocalAuthService.signup()`에도 있어 이론상 동일한 리스크를 안고 있음 — risk-analysis 담당 범위가 아니라 이번엔 고치지 않고 auth/admin 담당자에게 별도로 알리기로 함. 반면 `PropertyReportService.report()`는 재조회 없이 예외를 바로 비즈니스 에러로 매핑하는 방식이라 이 문제와 무관함.
12. **(2026-08-06 신규 이슈, 완전 해결) `POST /risk-analysis`가 실제로 발견된 신호가 있어도 `signalCount: 0`을 반환할 수 있던 문제 — MySQL REPEATABLE READ에서 실측 재현·수정.** 11번 수정 직후 "risk-analysis 관련 모든 소스에 사이드 이펙트가 있는지" 재검토하다가 발견. `checkAndSummarize()`(`POST /risk-analysis`가 호출)는 `checkAndSave()` 실행 직후 같은 트랜잭션 안에서 `getSignals()`를 다시 호출해 `signalCount`를 만들었는데, `checkAndSave()`가 처음 발견한 신호는 `PropertyRiskCheck`/`PropertyRisk`에 REQUIRES_NEW(별도 트랜잭션)로 insert된다 — 이 별도 트랜잭션은 바깥 트랜잭션보다 나중에 커밋되므로, MySQL REPEATABLE READ에서는 바깥 트랜잭션이 이미 그 전에(예: 맨 처음 매물 조회 시점) 고정해둔 스냅샷에 갇혀 방금 커밋된 그 행을 재조회에서 못 볼 수 있다. 즉 매물 하나에 대해 신호가 처음 발견되는 시점마다(사실상 대부분의 최초 계산) `signalCount`가 실제로는 리스크가 있는데도 0으로 잘못 나올 수 있는 문제였다. 이번엔 이론에서 그치지 않고 **H2를 `TransactionDefinition.ISOLATION_REPEATABLE_READ`로 강제해 실측 재현**함(`RepeatableReadVisibilityExperimentTest`) — REQUIRES_NEW로 커밋된 행이 바깥 트랜잭션의 재조회에서 실제로 0건으로 보이는 것을 직접 확인. 수정: `checkAndSave(Property)`/`checkAndSave(Long, Long)`가 `void` 대신 `int`(감지기 루프 안에서 이미 판정한 "리스크 발견" 개수)를 반환하도록 바꾸고, `checkAndSummarize()`는 이 반환값을 그대로 쓰며 더 이상 `getSignals()`를 재호출하지 않는다 — DB 재조회 자체를 없애 격리수준과 무관하게 항상 정확하다. 같은 테스트로 반환값(수정 후 방식)은 정확하고 재조회(구버전 방식)는 여전히 0을 보인다는 것을 동시에 확인해 대조군까지 남겨둠. `GET /risk-signals`(`getSignals()` 단독 호출)는 항상 새 트랜잭션이라 이 문제와 무관했음.
13. **(2026-08-19 신규 발견, 미해결)** PRICE_ANOMALY(시세 이상 가격)와 전세가율(보증금 안전성)이 서로 다른 시세 기준으로 독립 계산되다 보니 모순적으로 보이는 화면이 나올 수 있음 — PRICE_ANOMALY는 보증금을 주변 **전월세 실거래가**와 비교하고, 전세가율은 같은 보증금을 주변 **매매 시세**와 비교(보증금÷매매가)한다. 그런데 보증금이 시세보다 비정상적으로 싸서 PRICE_ANOMALY가 뜬 매물은, 그 "너무 싼" 보증금 자체가 분자로 들어가면서 전세가율은 오히려 낮게(=안전하게) 나온다 — 실사용 화면에서 "시세보다 90% 낮은 가격" 경고와 "전세가율 4%, 안전한 편" 배지가 동시에 뜨는 걸로 재현 확인함. 전세가율 계산이 "이 보증금 숫자 자체가 신뢰할 만한지"는 전혀 고려하지 않고 액면가만 나누기 때문 — 허위매물 미끼 가격일수록 역설적으로 전세가율은 더 안전해 보이게 됨. 아직 이슈/브랜치는 안 만들었고 인지만 해둔 상태 — 후속 조치 방향(예: 같은 매물에 PRICE_ANOMALY가 함께 떠 있으면 전세가율 "안전" 배지에 "가격 자체가 의심되니 참고만 하세요" 같은 경고 문구를 병기) 검토 필요.

## 전수조사 결과 (2026-08-12)

"완전 구현" 평가 자체는 대체로 정확했다 — 신호 탐지기 4종, API 4개, market-data 어댑터, `DepositSafetyCheckService`, 이벤트 기반 재계산까지 실제로 코드가 존재하고 소유권 검증도 일관되게 되어 있음을 코드로 직접 확인했다(`FakeListingSignalServiceTest`의 소유권 테스트들도 이를 뒷받침). 다만 아래 3건은 기존 "남은 이슈" 목록에 없던 새로 발견한 문제다.

### 버그/정확성

1. ~~**`FakeListingSignalService.checkAndSave(Property)`(service/FakeListingSignalService.java:141-153)가 신호 4종 upsert와 보증금 안전성 계산을 하나의 `@Transactional` 경계에 묶어놨다.** 141번째 줄부터 4개 탐지기를 판정·upsert하고, 마지막(151번째 줄)에 `depositSafetyCheckService.checkAndSave(property)`를 호출하는데 이 호출을 감싸는 try/catch가 없다. 만약 이 호출이(예: `MarketSaleComparisonService`가 의존하는 카카오 지오코딩/지역코드 클라이언트가 실패를 삼키지 않고 예외를 던지는 경우) 처리되지 않은 `RuntimeException`을 던지면, 이 메서드가 속한 트랜잭션 전체가 롤백된다 — 그런데 신호 4종 중 "기존 행이 있어 `overwrite()`로 dirty-check만 해둔" 결과는 같은 트랜잭션에 물려 있어 함께 롤백되지만, "신규 insert라 `REQUIRES_NEW`로 이미 별도 커밋된" 결과(174-209번째 줄 `upsertCheck`, 214-245번째 줄 `upsertRisk`)는 그대로 남아있게 된다.~~ — ✅ **(2026-08-14 해결)** `checkAndSave(Property)`가 `depositSafetyCheckService.checkAndSave(property)` 호출을 try/catch로 감싸 흡수하도록 수정(`RiskRecalculationService`와 동일한 fail-safe 원칙). `checkAndSaveAbsorbsDepositSafetyCheckFailure` 회귀 테스트로 확인함.
2. ~~**market-data 어댑터의 사유코드 매핑이 거래유형 미지원과 매물유형 미지원을 구분하지 못한다.** `MarketDataClientImpl.mapReason()`(client/MarketDataClientImpl.java:63-67)이 market-data의 `TRANSACTION_TYPE_UNSUPPORTED`(월세라 시세비교 대상이 아님)와 `PROPERTY_TYPE_UNSUPPORTED`(단독/다가구라 실거래 위치 비공개)를 둘 다 risk-analysis의 `MarketUnavailableReason.PROPERTY_TYPE_UNSUPPORTED`로 매핑한다.**~~ — ✅ **(2026-08-14 해결)** `MarketUnavailableReason`/`RiskCheckReason`에 `TRANSACTION_TYPE_UNSUPPORTED` 전용 값을 추가하고, `MarketDataClientImpl.mapReason()`/`PriceAnomalyDetector.mapReason()` 양쪽에서 매물유형 미지원과 분리해 매핑하도록 수정. 월세 매물의 판정불가 사유가 이제 "주택 유형 정보 부족"이 아니라 정확한 사유로 내려감.
3. ~~**`RiskPolicyConfig.jeonseRatioAlertOver`가 죽은 설정값이다.** `policy/RiskPolicyConfig.java:16`에 선언되고 `application.yml:104`(`jeonse-ratio-alert-over: 150`)에 값이 채워져 있지만, 코드 전체를 검색해도 `getJeonseRatioAlertOver()`를 호출하는 곳이 없다.**~~ — ✅ **(2026-08-14 해결)** `jeonseRatioWarnTo`와 값이 우연히 같아(150) 별도 임계값으로서 의미가 없다고 판단, 필드·yml값·테스트의 `ReflectionTestUtils.setField` 라인·`DepositSafetyCheckService`의 관련 주석까지 전부 정리(제거)함.

### 보안

위험 신호(`GET /risk-signals`, `POST /risk-analysis`)와 보증금 안전성(`GET /deposit-safety`, `POST /deposit-safety/recalculate`) 4개 엔드포인트 전부 서비스 레이어(`FakeListingSignalService.getSignals()`/`checkAndSave(userId, propertyId)`, `DepositSafetyCheckService.findOwnedProperty()`)에서 매물 존재/삭제/소유권을 확인하고 있어, 다른 사용자의 위험분석·전세가율 데이터에 접근할 수 있는 경로는 찾지 못했다. `DepositSafetyRecalculateRequest`도 `seniorDeposit`/`maxClaimAmount`에 `@PositiveOrZero` 검증이 걸려 있어 음수 입력 같은 기초적인 입력 검증도 되어 있다. `RiskRecalculationService`의 내부 이벤트 리스너(`onPropertyUpdated`)는 사용자 컨텍스트 없이 시스템이 트리거하는 재계산이라 소유권 체크를 건너뛰지만, API 경계 밖의 내부 로직이라 문제 없음. 이 도메인 범위에서 새로 발견된 보안 이슈는 없음.

### 코드 품질 (중복/구조/일관성)

1. ~~**`property.service.PropertyService`가 risk-analysis 리포지토리/엔티티를 직접 import한다.** 매물 목록에 위험신호 개수·전세가율 배지를 붙이기 위해(`PropertyService.java:30-33` import, `57-59`번째 줄 필드, `162-177`번째 줄 실사용) `riskanalysis.repository.PropertyRiskRepository`/`PropertyRiskCheckRepository`/`DepositSafetyCheckRepository`와 `riskanalysis.entity.PropertyRisk`, `riskanalysis.enums.DepositSafetyStatus`를 그대로 끌어다 쓴다. 쓰기(재계산 트리거)는 이벤트로 잘 분리돼 있지만 읽기(목록 요약)는 반대 방향으로 직접 결합돼 있다.**~~ — ✅ **(2026-08-18 해결)** market-data의 `MarketDataClient` 패턴과 동일하게, risk-analysis 쪽에 조회 전용 인터페이스 `PropertyRiskSummaryProvider`(+DTO `PropertyRiskSummary`, 구현체 `PropertyRiskSummaryProviderImpl`, 전부 `riskanalysis.client`/`riskanalysis.dto` 패키지)를 신설. `PropertyService`는 이제 이 인터페이스 하나만 참조하고, 3개 리포지토리·엔티티·enum import는 전부 제거됨. 어댑터 자체를 risk-analysis 쪽에 둔 이유는 risk-analysis가 이미 `Property` 엔티티를 참조하고 있어 이 방향의 의존이 새롭지 않은 반면, property는 risk-analysis를 전혀 몰라야 하기 때문. API 응답·동작은 변경 없음(순수 리팩토링).
2. ~~**낙관적 락 부재로 인한 update-vs-update 경쟁은 여전히 남아있다.** `DepositSafetyCheck`/`PropertyRisk`/`PropertyRiskCheck` 세 엔티티 모두 `@Version` 필드가 없다.**~~ — ✅ **(2026-08-14 해결)** 세 엔티티에 `@Version` 추가. 동시 갱신 충돌(`ObjectOptimisticLockingFailureException`) 시 `RiskRecalculationService`와 동일한 fail-safe 원칙으로 로그만 남기고 흡수하도록 처리 — 조회·수정·flush를 전부 REQUIRES_NEW 트랜잭션 안에서 끝내야 함(바깥 트랜잭션에서 읽은 엔티티를 mutate만 하고 별도 트랜잭션에서 flush하면, 바깥 세션이 그 엔티티를 여전히 dirty로 들고 있다가 다음 조회의 auto-flush 시점에 낡은 버전으로 재flush를 시도해 충돌하는 문제를 실제 동시성 테스트로 재현·확인함).

## (2026-08-19) 신호 메시지 톤 개선 — "판정" 대신 "정보 제공"

신호 탐지기 4종 중 2개(`DuplicateListingDetector`, `ShortTermRelistingDetector`)가 원래 **공개 매물 플랫폼(임대인/중개사가 매물을 올려 여러 세입자가 보는 구조)의 어뷰징 패턴**을 전제로 설계돼 있었는데, 이 서비스는 반대로 **세입자가 자기가 검토 중인 매물을 개인 계정에 비공개로 등록**하는 구조라는 게 대화 중 다시 짚였다. 두 신호 다 원래 전제가 이 서비스엔 안 맞아서, "의심된다"고 시스템이 단정하지 않고 비교 가능한 사실만 보여주는 쪽으로 톤을 바꿨다.

- **`DuplicateListingDetector`** — 동일 유저의 자기 자신 중복 등록은 `PropertyService.register()`가 이미 막고 있어(`PROPERTY_DUPLICATE`), 이 신호가 실제로 걸리는 경우는 **항상 다른 계정**이다. 이 서비스에서 "같은 매물을 다른 계정도 등록했다"는 건 대부분 "여러 명이 같은 매물에 관심 있다"는 정상적인 상황이라, 원래 있던 고정 문구("동일 주소로 등록된 다른 매물이 있어요")를 없애고, 비교 대상(가장 최근에 등록된 다른 매물)의 가격·등록 시점을 담은 문장으로 바꿨다(예: `"동일 주소로 등록된 다른 매물이 있어요 — 보증금 250,000,000원, 3일 전 등록"`). 가격이 다르다고 곧바로 허위매물로 보기도 어렵다(안 나가서 가격을 내렸을 수도 있음) — 그래서 "얼마나 다르면 의심"이라는 임계값을 시스템이 판단하지 않고 사용자에게 맡기기로 했다. `PropertyRepository`의 `existsByIdNotAnd...` 2개를 `findAllByIdNotAnd...OrderByCreatedAtDesc`로 교체(단순 존재 여부가 아니라 비교용 엔티티 자체가 필요해짐).
- **`ShortTermRelistingDetector`** — "삭제 후 재등록"으로 신규 매물처럼 보이게 숨기는 어뷰징 패턴은 애초에 **공개 플랫폼에서만 의미가 있다** — 다른 사람이 볼 수 있어야 "새 매물처럼 보이게" 숨기는 효과가 있는데, 이 서비스는 내가 등록한 매물을 나만 본다. 그래서 동일 유저가 삭제 후 비슷한 조건으로 재등록하는 것도 실제로는 등록 시 오타 수정, 마음이 바뀌어 다시 고려하는 경우가 대부분일 것으로 보인다. 고정 문구("이 매물, 최근에 삭제됐다가 비슷한 조건으로 다시 등록됐어요")를 며칠 전 삭제됐는지 담은 문장으로 바꿨다(예: `"이 매물, 5일 전 삭제됐다가 비슷한 조건으로 다시 등록됐어요"`).
- **`PriceAnomalyDetector`**는 다른 사용자 데이터가 아니라 국토부 실거래가(시세)와 비교하는 구조라 같은 문제가 없어 그대로 둠. `DepositSafetyCheckService`(전세가율)·소유권 변경 보조 신호도 매물 자체의 객관적 정보만 써서 마찬가지로 문제 없음.
- **`SameAccountMultipleDetector`**도 원래 임대인/중개사 어뷰징 탐지 의도로 설계돼 이 서비스 구조와 안 맞는 건 동일하지만, 이건 애초에 `multiAccountDetectionEnabled: false`로 꺼져있는 상태라 이번엔 손대지 않고 admin 담당자와의 논의로 넘김(팀 결정 대기 상태 유지).

FE는 변경 없음 — `description`이 원래도 자유 텍스트 필드라, 문장 내용만 바뀌었을 뿐 응답 구조(DTO)는 그대로다.

## (2026-08-20) `RiskSignalResponse`에 후속 행동 안내(`recommendedActions`) 추가

FE 피드백(7-1) — `PriceAnomalyDetector`의 메시지가 "시세보다 X% 낮은 가격이에요 — 왜 이렇게 저렴한지 확인해보세요"까지만 있고, **뭘 확인해야 하는지**가 없어 실제 행동으로 이어지기 어렵다는 지적. 두 가지 안(A: 문자열 하나에 다 욱여넣기, B: 구조화된 필드로 분리) 중 FE와 협의해 B안으로 결정 — 신호 카드가 이미 두 화면(`RiskAnalysisClient.tsx`, `PropertyDetailClient.tsx`)에서 공유되는 구조라 FE 작업량이 크지 않다는 판단.

- `RiskSignalResponse`에 `List<String> recommendedActions` 필드 추가. PRICE_ANOMALY 전용으로 하지 않고 신호 공통 필드로 둠(FE 제안) — 나중에 다른 신호 타입에도 안내가 필요해지면 스키마를 또 안 바꿔도 됨. 지금은 `PRICE_ANOMALY`만 5개 항목(동일 지역 실거래가 확인/등기부등본 확인/주변 동일 면적 매물 가격 비교/중개사에게 가격 낮은 이유 확인/옵션·관리비 등 추가 비용 확인)이 채워지고, 나머지 3개 신호 타입은 빈 리스트.
- **DB에 저장하지 않음** — 매물마다 달라지는 내용이 아니라 신호 타입에 고정된 일반 안내라, `PropertyRisk` 엔티티/스키마를 안 건드리고 `RiskSignalResponse.from()`에서 signalType 기준으로 정적 매핑만 한다. 리스크가 실제로 발견된 경우(`risk != null`)에만 채워지고, 판정불가/리스크 없음이면 빈 리스트.
- 조사해본 결과, 제안된 5개 행동 중 실제로 이 앱이 지원하는 건 "관리비 확인"(`Property.maintenanceFee` 필드로 이미 등록 시점에 받고 있음)뿐이고, 나머지(실거래가 조회, 주변 매물 비교, 등기부등본, 중개사 문의)는 앱 안에 대응 기능이 없어 사용자가 앱 밖에서 직접 해야 하는 행동이다 — 그래도 "무엇을 확인해야 하는지" 구체적으로 짚어주는 것 자체가 피드백의 요구사항이라 그대로 포함시킴.
- FE 작업(타입 추가 + 두 컴포넌트에 체크리스트 렌더링)은 이 스키마 확정 이후 별도로 진행.